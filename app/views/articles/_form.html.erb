<%= simple_form_for @article do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :photo, as: :file %>
  <%= f.input :video, as: :file %>

  <div>
    <%= f.button :submit %>
  </div>
<% end %>
